# Drop your resume here

Place your PDF at `public/resume.pdf`. Any link to `/resume.pdf` in the site (nav, hero CTA, footer, `resume` terminal command) will serve it.

**Recommended filename:** `resume.pdf` (exact).

**Suggested source:** the Cribl-tailored resume read by the scaffolder on first run — `Indu Lohith Narisetty — Sr SecOps.pdf`. Save as `public/resume.pdf` after you export from Word / Google Docs.

**OG preview:** the site uses `public/og.svg` for LinkedIn / Twitter preview cards. Replace it with `og.png` if your target crawler rejects SVG OG — LinkedIn has historically wanted raster.
